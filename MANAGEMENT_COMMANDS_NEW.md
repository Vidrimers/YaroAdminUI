# Управление сервером - Доступные команды

После успешной аутентификации (SSH, WebAuthn или Telegram) вы получаете доступ к панели управления сервером.

## 🎯 Доступные команды и функции

### 1. **Перезагрузить SSH** (restart-ssh)

- **Описание:** Перезагружает SSH демон на сервере
- **Команда:** `systemctl restart sshd`
- **Кнопка:** В карточке "Управление системой"
- **Результат:** SSH сервис перезапускается, текущие соединения могут быть разорваны

### 2. **Проверить диск** (check-disk)

- **Описание:** Проверяет свободное место и использование дискового пространства
- **Команда:** `df -h`
- **Кнопка:** В карточке "Управление системой"
- **Результат:** Показывает статистику дискового пространства (в удобном формате)
- **Пример вывода:**
  ```
  Filesystem      Size  Used Avail Use% Mounted on
  /dev/sda1       200G   50G  150G  25% /
  /dev/sda2       500G  200G  300G  40% /home
  ```

### 3. **Перезагрузить сервис** (restart-service)

- **Описание:** Перезагружает основной веб-сервис (Nginx)
- **Команда:** `systemctl restart nginx`
- **Кнопка:** В карточке "Управление системой"
- **Результат:** Веб-сервис перезапускается, текущие HTTP соединения могут быть разорваны

## 🚀 Управление Сервисами

### Systemctl Сервисы

Карточка "Сервисы" показывает список всех активных systemctl сервисов на сервере:

- **SSH** (sshd)
- **Web Server** (nginx)
- **Database** (mysql, postgresql, mongodb и др.)
- **Redis, Cache** и другие важные сервисы

**Для каждого сервиса доступны кнопки:**

- 🟢 **Запуск** - Запустить сервис (`systemctl start <name>`)
- 🔄 **Перезагрузка** - Перезагрузить сервис (`systemctl restart <name>`)
- 🔴 **Остановка** - Остановить сервис (`systemctl stop <name>`)

**Статус сервиса:**

- ✓ Запущен (зелёный) - сервис работает
- ✕ Остановлен (красный) - сервис выключен

### PM2 Процессы

Если у вас установлен PM2, панель также покажет все управляемые им процессы:

- **Название процесса**
- **PID** (Process ID)
- **Memory** (использование памяти)
- **CPU** (использование процессора)
- **Статус** (Online/Offline)

**Для каждого PM2 процесса доступны кнопки:**

- 🟢 **Запуск** - Запустить процесс (`pm2 start <name>`)
- 🔄 **Перезагрузка** - Перезагрузить процесс (`pm2 restart <name>`)
- 🔴 **Остановка** - Остановить процесс (`pm2 stop <name>`)

**Пример PM2 процессов:**

```
adminui-server (PID: 1234, Memory: 125MB, CPU: 2%, Online)
api-backend (PID: 5678, Memory: 256MB, CPU: 5%, Online)
telegram-bot (PID: 2468, Memory: 85MB, CPU: 1%, Online)
```

## 📜 Выполнение скриптов (.sh)

Карточка "Скрипты" показывает список всех доступных bash-скриптов на сервере.

**Где ищутся скрипты:**

- `/home/*/scripts/` - скрипты в домашних директориях
- `/opt/scripts/` - стандартная директория для скриптов
- `/root/scripts/` - скрипты root пользователя
- `./scripts/` - скрипты в текущей директории приложения
- `/usr/local/bin/` - скрипты в системном bin

**Как выполнить скрипт:**

1. Найдите скрипт в списке
2. Нажмите кнопку "▶ Выполнить скрипт"
3. Система выполнит скрипт с помощью `bash`
4. Получите результат (успех или ошибка)

**Пример скриптов:**

```
📄 /opt/scripts/backup.sh
📄 /opt/scripts/cleanup.sh
📄 /home/user/scripts/deploy.sh
📄 /root/scripts/maintenance.sh
```

**Безопасность скриптов:**

- Скрипты выполняются из разрешённых директорий только
- Невозможно выполнить скрипт с использованием `../` (path traversal)
- Максимальное время выполнения: 30 секунд
- Все выполнения логируются

## 🔐 Безопасность

Все команды и операции защищены несколькими уровнями:

1. **JWT токен** - команда может быть выполнена только аутентифицированным пользователем
2. **Whitelist** - разрешены только определённые безопасные команды
3. **Whitelist сервисов** - для systemctl разрешены только важные сервисы
4. **Path validation** - для скриптов проверяется правильность пути
5. **Логирование** - все выполненные команды логируются в базу данных (таблица `activity_logs`)

## 📊 Доступная информация в Статус сервера

Помимо команд, в панели отображается:

### Server Status (Статус сервера)

- IP адрес сервера
- Статус подключения (online/offline)
- Uptime (время работы)
- CPU использование (%)
- RAM использование (%)

### SSH Keys (SSH ключи)

- Просмотр добавленных ключей
- Удаление ключей
- Добавление новых ключей

### Firewall (Брандмауэр)

- Управление правилами
- Открытие/закрытие портов

### Activity Logs (Логи активности)

- История всех команд
- История входов
- Временные метки и пользователи

### Notifications (Уведомления)

- Системные уведомления
- Оповещения об ошибках

## 🔄 Как использовать

### Быстрая проверка здоровья сервера

1. Смотрите **Server Status** - CPU, RAM, Uptime
2. Нажимаете **"Проверить диск"** - проверка дискового пространства
3. Смотрите **Сервисы** - статус всех важных сервисов
4. Проверяете **Activity Logs** - нет ли подозрительной активности

### Перезагрузка сервиса

1. Откройте карточку **"Сервисы"**
2. Найдите нужный сервис (например, nginx)
3. Нажмите **"Перезагрузка"**
4. Дождитесь тоста "nginx - restart выполнен"

### Запуск скрипта развёртывания

1. Откройте карточку **"Скрипты"**
2. Найдите скрипт (например, `/opt/scripts/deploy.sh`)
3. Нажмите **"▶ Выполнить скрипт"**
4. Дождитесь выполнения

## ⚠️ Важные примечания

- Команды выполняются с учётом прав текущего пользователя
- Если операция требует `sudo`, она может не выполниться (требуется настройка sudoers)
- При ошибке выполнения команды будет показано сообщение об ошибке
- Все операции логируются для аудита безопасности
- PM2 должен быть установлен на сервере: `npm install -g pm2`
- Для поиска скриптов используется команда `find` - убедитесь, что она доступна

## 🚀 Расширение функционала

### Добавление новой системной команды

В `adminui.js` найдите объект `allowedCommands` в методе `POST /api/server/execute`:

```javascript
const allowedCommands = {
  "restart-ssh": {
    cmd: "systemctl restart sshd",
    desc: "SSH service restarted",
  },
  "check-disk": { cmd: "df -h", desc: "Disk usage check" },
  "restart-service": {
    cmd: "systemctl restart nginx",
    desc: "Service restarted",
  },
  // Добавьте новую команду здесь:
  "check-memory": { cmd: "free -h", desc: "Memory usage check" },
};
```

Затем добавьте кнопку в `index.html` в карточку "Управление системой":

```html
<button class="btn btn-info" data-action="check-memory">
  📊 Проверить память
</button>
```

### Добавление новой службы в whitelist

В `POST /api/server/manage-service` найдите `serviceWhitelist`:

```javascript
const serviceWhitelist = [
  "sshd",
  "nginx",
  "mysql",
  "postgresql",
  "redis",
  "mongodb",
  "apache2",
  "httpd",
];
// Добавьте новую службу:
serviceWhitelist.push("docker");
```

## 📝 Примеры использования в скриптах

Вы можете вызывать API прямо из ваших скриптов:

```bash
#!/bin/bash

# Отправить уведомление на сервер
TOKEN="ваш_jwt_токен"
curl -X POST http://localhost:666/api/server/manage-service \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"type":"systemctl","name":"nginx","action":"restart"}'

# Проверить статус сервиса
curl -X GET http://localhost:666/api/server/status \
  -H "Authorization: Bearer $TOKEN"
```

---

**Версия:** 2.0.0  
**Последнее обновление:** 2024  
**Автор:** YaroAdminUI Team
