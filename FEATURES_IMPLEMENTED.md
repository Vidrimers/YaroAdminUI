# ✅ Реализация управления сервисами и скриптами

## 📝 Краткое резюме

Я добавил полноценную систему управления сервисами (systemctl + PM2) и выполнения bash-скриптов прямо из админ-панели.

---

## 🎯 Что было добавлено

### 1. **Новые Endpoints на сервере** (adminui.js)

#### `POST /api/server/services` (обновлено)

Получает список всех systemctl сервисов и PM2 процессов:

```javascript
// Возвращает:
{
  systemctl: [
    { type: "systemctl", name: "sshd", status: "active", description: "OpenSSH Server" },
    { type: "systemctl", name: "nginx", status: "active", description: "Web Server" },
    // ...
  ],
  pm2: [
    { type: "pm2", name: "adminui-server", status: "online", pid: 1234, memory: "125MB", cpu: "2%" },
    // ...
  ]
}
```

#### `POST /api/server/manage-service` (новое)

Управляет сервисами и PM2 процессами:

```javascript
// Запрос:
{
  type: "systemctl" | "pm2",
  name: "nginx",
  action: "start" | "stop" | "restart" | "reload" | "status"
}

// Ответ:
{
  success: true,
  type: "systemctl",
  name: "nginx",
  action: "restart",
  output: "Command executed successfully"
}
```

**Безопасность:**

- Whitelist сервисов: sshd, nginx, mysql, postgresql, redis, mongodb, apache2, httpd
- Только разрешённые действия: start, stop, restart, reload, status

#### `GET /api/server/scripts` (новое)

Получает список доступных bash-скриптов:

```javascript
// Возвращает:
{
  scripts: [
    { path: "/opt/scripts/backup.sh", name: "backup.sh", directory: "/opt/scripts" },
    { path: "/opt/scripts/deploy.sh", name: "deploy.sh", directory: "/opt/scripts" },
    // ...
  ],
  count: 5
}
```

**Где ищет скрипты:**

- `/home/*/scripts/`
- `/opt/scripts/`
- `/root/scripts/`
- `./scripts/`
- `/usr/local/bin/`

#### `POST /api/server/execute-script` (новое)

Выполняет выбранный скрипт:

```javascript
// Запрос:
{ scriptPath: "/opt/scripts/backup.sh" }

// Ответ:
{
  success: true,
  scriptPath: "/opt/scripts/backup.sh",
  output: "Backup completed successfully..."
}
```

**Безопасность:**

- Только из разрешённых директорий
- Блокировка path traversal (`../`)
- Максимальное время выполнения: 30 секунд
- Все выполнения логируются

---

### 2. **Новые методы в API сервисе** (adminui_client.js)

```javascript
// Получить список сервисов и PM2
await this.api.getServices();

// Управлять сервисом
await this.api.manageService(type, name, action);
// Пример: await this.api.manageService("systemctl", "nginx", "restart")

// Получить список скриптов
await this.api.getScripts();

// Выполнить скрипт
await this.api.executeScript(scriptPath);
// Пример: await this.api.executeScript("/opt/scripts/backup.sh")
```

---

### 3. **Обновленный UI** (index.html + style.css)

#### Новая карточка "🚀 Сервисы"

Разделена на две секции:

**Systemctl сервисы:**

```
┌─────────────────────────────────────────┐
│ nginx                                   │
│ [Запуск] [Перезагрузка] [Остановка]     │
│ ✓ Запущен                               │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ sshd                                    │
│ [Запуск] [Перезагрузка] [Остановка]     │
│ ✓ Запущен                               │
└─────────────────────────────────────────┘
```

**PM2 процессы:**

```
┌─────────────────────────────────────────┐
│ adminui-server                          │
│ PID: 1234 | Memory: 125MB | CPU: 2%    │
│ [Запуск] [Перезагрузка] [Остановка]     │
│ ✓ Online                                │
└─────────────────────────────────────────┘
```

#### Новая карточка "📜 Скрипты"

```
┌─────────────────────────────────────────┐
│ 📄 /opt/scripts/backup.sh               │
│ [▶ Выполнить скрипт]                    │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ 📄 /opt/scripts/deploy.sh               │
│ [▶ Выполнить скрипт]                    │
└─────────────────────────────────────────┘
```

---

### 4. **Обработчики событий** (adminui_client.js)

#### `loadServices()` - Загружает все сервисы

- Получает systemctl сервисы и PM2 процессы
- Отображает статус каждого
- Добавляет кнопки управления

#### `loadScripts()` - Загружает все скрипты

- Ищет скрипты в разрешённых директориях
- Отображает полный путь
- Добавляет кнопки выполнения

#### `setupServiceControls()` - Обработчик кнопок сервисов

```javascript
btn.click → manageService(type, name, action)
         → показывает "⏳ Выполняется..."
         → выполняет команду
         → перезагружает список сервисов
         → показывает результат
```

#### `setupScriptControls()` - Обработчик кнопок скриптов

```javascript
btn.click → executeScript(scriptPath)
         → показывает "⏳ Выполняется..."
         → выполняет скрипт
         → показывает результат
         → логирует в Activity Logs
```

---

### 5. **Новые CSS стили** (style.css)

```css
.services-list         /* Контейнер для списка сервисов */
/* Контейнер для списка сервисов */
.service-item         /* Элемент сервиса */
.script-item          /* Элемент скрипта */
.service-actions      /* Контейнер для кнопок */
.pm2-info             /* Информация о PM2 */
.pm2-info-item        /* Отдельная информация */
.script-path          /* Путь к скрипту */
.status-active        /* Статус активный */
.status-inactive; /* Статус неактивный */
```

---

## 🔄 Как всё работает

### Сценарий 1: Просмотр и управление сервисами

```
1. Панель загружается
   ↓
2. loadServices() вызывает GET /api/server/services
   ↓
3. Сервер выполняет:
   - systemctl list-units --type=service --all --output=json
   - pm2 list --json
   ↓
4. Клиент отображает:
   - Список systemctl сервисов с кнопками управления
   - Список PM2 процессов с информацией о ресурсах
   ↓
5. Пользователь нажимает "Перезагрузка" для nginx
   ↓
6. Отправляется POST /api/server/manage-service
   {type: "systemctl", name: "nginx", action: "restart"}
   ↓
7. Сервер выполняет: systemctl restart nginx
   ↓
8. Клиент показывает тост: "nginx - restart выполнен"
   ↓
9. Список сервисов перезагружается автоматически
```

### Сценарий 2: Выполнение скрипта

```
1. Панель загружается
   ↓
2. loadScripts() вызывает GET /api/server/scripts
   ↓
3. Сервер выполняет find для поиска всех .sh файлов
   ↓
4. Клиент отображает список скриптов
   ↓
5. Пользователь нажимает "▶ Выполнить" для /opt/scripts/backup.sh
   ↓
6. Отправляется POST /api/server/execute-script
   {scriptPath: "/opt/scripts/backup.sh"}
   ↓
7. Сервер выполняет: bash "/opt/scripts/backup.sh"
   ↓
8. Сервер логирует в activity_logs: "Executed script: /opt/scripts/backup.sh"
   ↓
9. Клиент показывает тост с результатом
   ↓
10. Activity Logs автоматически обновляются
```

---

## 🔐 Безопасность

### Уровни защиты

1. **JWT токен** - Только аутентифицированные пользователи
2. **Whitelist команд** - Для systemctl разрешены только:
   - sshd, nginx, mysql, postgresql, redis, mongodb, apache2, httpd
3. **Whitelist директорий** - Для скриптов разрешены только:
   - /home/\*/scripts/, /opt/scripts/, /root/scripts/, ./scripts/, /usr/local/bin/
4. **Path validation** - Блокировка path traversal (../)
5. **Timeout** - Максимум 30 секунд на выполнение скрипта
6. **Логирование** - Все операции записываются в activity_logs

---

## 📊 Примеры использования

### Создание собственного скрипта развёртывания

1. Создайте файл `/opt/scripts/deploy.sh`:

```bash
#!/bin/bash
echo "Stopping application..."
pm2 stop adminui-server
echo "Pulling latest code..."
cd /path/to/app && git pull origin main
echo "Installing dependencies..."
npm install
echo "Starting application..."
pm2 start /path/to/app/adminui.js --name adminui-server
echo "Deployment completed!"
```

2. Откройте админ-панель
3. Перейдите на карточку "Скрипты"
4. Нажмите "▶ Выполнить" для `/opt/scripts/deploy.sh`
5. Смотрите результат выполнения

### Управление Nginx

**Сценарий:** Обновили конфигурацию nginx, нужно применить

1. Откройте карточку "Сервисы"
2. Найдите "nginx" в списке systemctl
3. Нажмите "Перезагрузка"
4. Система выполнит: `systemctl restart nginx`
5. Видите подтверждение в тосте

### Мониторинг PM2 приложений

1. Откройте карточку "Сервисы"
2. Скроллите вниз до "PM2 процессы"
3. Видите:

   - Какие приложения запущены
   - Сколько памяти они используют
   - Нагрузку на CPU
   - Статус каждого приложения

4. Если приложение зависло:
   - Нажимаете "Перезагрузка"
   - PM2 перезапустит приложение

---

## 📝 Документация

Созданы два новых файла документации:

1. **MANAGEMENT_COMMANDS_NEW.md** - Полный справочник по всем командам и функциям
2. **WHAT_CAN_I_DO_NEW.md** - Введение для новых пользователей

Прочитайте эти файлы для подробной информации!

---

## 🚀 Что дальше?

Вы можете:

1. **Добавить новые сервисы в whitelist**

   - Отредактируйте `serviceWhitelist` в `POST /api/server/manage-service`

2. **Добавить новые системные команды**

   - Отредактируйте `allowedCommands` в `POST /api/server/execute`
   - Добавьте кнопку в index.html

3. **Создавать свои скрипты**

   - Добавьте .sh файлы в `/opt/scripts/`
   - Они будут видны в админ-панели

4. **Интегрировать с мониторингом**
   - Используйте логи из Activity Logs
   - Настройте уведомления при ошибках

---

## ✨ Результат

Теперь ваша админ-панель поддерживает:

✅ Просмотр всех systemctl сервисов  
✅ Управление сервисами (start/stop/restart)  
✅ Просмотр всех PM2 процессов с информацией о ресурсах  
✅ Управление PM2 приложениями  
✅ Автоматический поиск и выполнение bash-скриптов  
✅ Безопасное выполнение скриптов с контролем времени  
✅ Полное логирование всех операций  
✅ Интуитивный пользовательский интерфейс

**Вы теперь можете полностью управлять сервером прямо из браузера! 🎉**

---

**Версия:** 2.0.0  
**Дата:** 2024  
**Статус:** ✅ Готово к использованию
