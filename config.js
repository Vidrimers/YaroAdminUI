// Configuration for AdminUI
const CONFIG = {
  // Server URL - change this to your server IP and port
  SERVER_URL: window.location.origin, // Uses current domain
  // Or specify directly:
  // SERVER_URL: 'http://144.124.237.222:666'
  
  // Таймауты (в миллисекундах)
  API_TIMEOUT: 5000,
  
  // Интервал обновления данных dashboard (в миллисекундах)
  REFRESH_INTERVAL: 10000,
  
  // Автоматическое обновление логов
  AUTO_REFRESH_LOGS: true,
};
