# 🚀 Quick Start: Remote Server Management

## Step 1: Configure SSH Key

Ты должен иметь SSH ключ для подключения к серверу 144.124.237.222.

### Вариант A: Если уже есть SSH ключ

Убедись что в `.env` указан путь:

```bash
SSH_KEY_PATH=~/.ssh/id_rsa
```

### Вариант B: Если нужно создать новый ключ

```bash
ssh-keygen -t rsa -f ~/.ssh/id_rsa -N ""
```

Затем скопируй публичный ключ на сервер:

```bash
ssh-copy-id -i ~/.ssh/id_rsa root@144.124.237.222
```

## Step 2: Проверить SSH соединение

```bash
ssh -i ~/.ssh/id_rsa root@144.124.237.222 "uptime"
```

Должно вывести что-то вроде:

```
up 45 days, 3:24
```

## Step 3: Запустить AdminUI

```bash
npm run both
```

Это запустит:

- Web UI на `http://localhost:666`
- Telegram Bot

## Step 4: Войти в админку

1. Открой http://localhost:666
2. Выбери метод входа:
   - SSH Key (SSH криптография)
   - WebAuthn (отпечаток пальца/Face ID)
   - Telegram Bot

## Step 5: Управлять сервером

После входа ты сможешь:

### 🖥️ Смотреть статус сервера

- Uptime
- RAM usage
- CPU cores

### 🚀 Управлять сервисами

- Просмотр systemctl сервисов (nginx, mysql, etc)
- Просмотр PM2 приложений
- Restart/start/stop сервисы

### 📜 Запускать скрипты

- Автоматический поиск `.sh` скриптов на сервере
- Запуск с одного клика

### 🔥 Управлять файрволом

- Открыть/закрыть порты через UFW
- Просмотр правил

## Структура команд

**Все команды выполняются на удалённом сервере** (144.124.237.222), НЕ на твоём компьютере!

### Примеры

Клик на "Restart nginx" → SSH команда → Выполняется на 144.124.237.222

```
ТВЕ ЩА МАШИНА          УДАЛЁННЫЙ СЕРВЕР
┌─────────┐             ┌─────────────┐
│ AdminUI │──────SSH───→│ 144.124.... │
│ Browser │←────Output─│ nginx       │
└─────────┘             └─────────────┘
```

## Важные детали

⚠️ **ОБЫЧНО ОШИБКИ:**

1. **"Command not allowed"**

   - SSH соединение есть, но команда не в белом списке
   - Проверь какую команду пытаешься выполнить

2. **"SSH Connection timeout"**

   - Проверь что сервер доступен:

   ```bash
   ping 144.124.237.222
   ```

3. **"Permission denied"**
   - Проверь SSH ключ:
   ```bash
   ssh-keygen -l -f ~/.ssh/id_rsa.pub
   ```
   - Убедись что он добавлен на сервер:
   ```bash
   ssh root@144.124.237.222 "cat ~/.ssh/authorized_keys"
   ```

## Логирование

Все действия записываются в базу данных `adminUI.db`:

```bash
sqlite3 adminUI.db "SELECT * FROM activity_logs ORDER BY timestamp DESC LIMIT 5;"
```

## Переменные окружения

В `.env` файле есть:

```
SERVER_IP=144.124.237.222      # Адрес сервера
SSH_KEY_PATH=~/.ssh/id_rsa    # Путь к ключу
SSH_PORT=22                     # SSH порт
SSH_USERNAME=root               # Пользователь на сервере
```

Если нужно изменить сервер - измени `SERVER_IP` в `.env`.

## Готово! 🎉

Ты готов управлять удалённым сервером!

Какие вопросы?
