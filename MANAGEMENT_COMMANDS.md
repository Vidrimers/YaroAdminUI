# Управление сервером - Доступные команды

После успешной аутентификации (SSH, WebAuthn или Telegram) вы получаете доступ к панели управления сервером.

## 🎯 Доступные команды

### 1. **Перезагрузить SSH** (restart-ssh)

- **Описание:** Перезагружает SSH демон на сервере
- **Команда:** `systemctl restart sshd`
- **Кнопка:** В карточке "System Commands"
- **Результат:** SSH сервис перезапускается, текущие соединения могут быть разорваны

### 2. **Проверить диск** (check-disk)

- **Описание:** Проверяет свободное место и использование дискового пространства
- **Команда:** `df -h`
- **Кнопка:** В карточке "System Commands"
- **Результат:** Показывает статистику дискового пространства (в удобном формате)
- **Пример вывода:**
  ```
  Filesystem      Size  Used Avail Use% Mounted on
  /dev/sda1       200G   50G  150G  25% /
  /dev/sda2       500G  200G  300G  40% /home
  ```

### 3. **Перезагрузить сервис** (restart-service)

- **Описание:** Перезагружает основной веб-сервис (Nginx)
- **Команда:** `systemctl restart nginx`
- **Кнопка:** В карточке "System Commands"
- **Результат:** Веб-сервис перезапускается, текущие HTTP соединения могут быть разорваны

## 🔐 Безопасность

Все команды выполняются с проверкой:

1. **JWT токен** - команда может быть выполнена только аутентифицированным пользователем
2. **Whitelist** - разрешены только определённые безопасные команды
3. **Логирование** - все выполненные команды логируются в базу данных (таблица `activity_logs`)

## 📊 Доступная информация

Помимо команд, в панели отображается:

### Server Status (Статус сервера)

- IP адрес сервера
- Статус подключения (online/offline)
- Uptime (время работы)
- CPU использование (%)
- RAM использование (%)

### SSH Keys (SSH ключи)

- Просмотр добавленных ключей
- Удаление ключей
- Добавление новых ключей

### Firewall (Брандмауэр)

- Управление правилами
- Открытие/закрытие портов

### Activity Logs (Логи активности)

- История всех команд
- История входов
- Временные метки и пользователи

### Notifications (Уведомления)

- Системные уведомления
- Оповещения об ошибках

## 🔄 Как выполнить команду

1. Войдите в систему (SSH, WebAuthn или Telegram)
2. Перейдите на вкладку "System Commands"
3. Нажмите на нужную кнопку команды
4. Дождитесь результата (появится тост с уведомлением)
5. Результат отобразится в нижней части карточки

## ⚠️ Примечания

- Команды выполняются с учётом прав текущего пользователя
- Если операция требует `sudo`, она может не выполниться (требуется настройка sudoers)
- При ошибке выполнения команды будет показано сообщение об ошибке
- Все операции логируются для аудита безопасности

## 🚀 Расширение функционала

Новые команды можно добавить в `adminui.js`:

```javascript
// В объекте allowedCommands в методе POST /api/server/execute
const allowedCommands = {
  "your-command": { cmd: "your-shell-command", desc: "Your description" },
  // ...
};
```

Затем добавьте кнопку в `index.html` в карточку "System Commands":

```html
<button class="btn btn-primary" data-action="your-command">Ваша команда</button>
```

## 📝 Примеры использования

### Полная проверка системы

1. Нажмите "Проверить диск" - узнайте состояние дискового пространства
2. Посмотрите Server Status - проверьте CPU и RAM
3. Проверьте Activity Logs - убедитесь, что нет подозрительной активности

### Обслуживание сервера

1. Нажмите "Перезагрузить SSH" - обновите SSH конфигурацию
2. Дождитесь уведомления об успешном выполнении
3. Переподключитесь если потребуется

---

**Версия:** 1.0.0  
**Последнее обновление:** 2024  
**Автор:** YaroAdminUI Team
